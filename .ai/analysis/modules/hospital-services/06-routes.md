# Routes
Resource `/admin/hospital-services` excludes `show` and includes index, create,
store, edit, update and destroy. PATCH endpoints
`/{hospital_service}/activate` and `/deactivate` manage status.

