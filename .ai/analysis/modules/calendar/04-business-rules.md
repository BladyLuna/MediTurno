# Business Rules
Include `assigned` and `changed`, exclude `cancelled` and soft-deleted; use actual `start_at/end_at`; effective color/name/code inherit service override then global template; updates occur by reload/query, not WebSockets.
