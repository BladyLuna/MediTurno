# Flow
Admin submits staff data, validation enforces service and role-compatible user,
transaction persists and audits. Jefe requests `/service-staff`; backend derives
managed service IDs and returns only matching staff.

