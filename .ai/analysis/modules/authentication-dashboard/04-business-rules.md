# Business Rules
- `CONFIRMED`: roles are `admin`, `jefe_servicio`, `personal` in `users.role` (DEC-001).
- `CONFIRMED`: protected routes require authenticated and active users.
- `CONFIRMED`: no external permission package is used.
- `PENDING CONFIRMATION`: current login throttling; CLAUDE describes it as work to implement.

