# Flow
Admin manages a global template, then associates it to a service with optional
overrides. Validation rejects one-sided custom times; consumers resolve custom
values first and inherit global values otherwise.

