# Users

## Objective
Administer accounts, roles, activation and logical deletion.

## Evidence
`CONFIRMED`: FEAT-002; DEC-001 and DEC-009; admin user routes and controllers.

## Dependencies
`User`, `UserPolicy`, FormRequests, `AuditLogService`, transactions and SoftDeletes.

