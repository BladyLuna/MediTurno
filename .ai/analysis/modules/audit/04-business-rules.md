# Business Rules
Audit starts with early critical operations; records user, action, model type/id, old/new values, IP, user agent and creation time; reports are not audited; writes should share transaction with critical change.
